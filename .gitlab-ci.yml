image: docker:stable
stages:
  - build
  - push
build:
  stage: build
  services:
   - docker:dind
  script:
    - docker build -t zeemyself/rn-android .
push:
  stage: push
  script:
    - docker push zeemyself/rn-android
  when: manual

